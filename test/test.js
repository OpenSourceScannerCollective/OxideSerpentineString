
var myvar = "string"

var myvar = 123;

var myvar = "string with an inline comment no. 1"; /* inline comment for string no. 1*/
var myvar = "string with an inline comment no. 2"; // inline comment for string no. 2

/* single-line comment */

/*
    This is a multi-
    -line comment.
*/

// single-line comment

var myvar = "string"; // inline-comment

"string \"with\" escaped chars 1"

'string \'with\' escaped chars 2'

"nested 'strings 1' without escapes 3"

'nested \"strings 2\" without escapes 4'

"multiple 'nested' \"strings\" with different enclosure literals"

'multiple \'nested\' \"strings\" with different enclosure literals'

 `this is a single-line string`

var myvar = `this is a single-line string`;

`this is a multi-
-line string
using back-ticks`

var myvar = `this is a multi-
-line string
using back-ticks`

var secret = "-----BEGIN RSA PRIVATE KEY-----"
var secret = "-----BEGIN DSA PRIVATE KEY-----"
var secret = "-----BEGIN EC PRIVATE KEY-----"
var secret = "-----BEGIN PGP PRIVATE KEY BLOCK-----"
